# 0001 — read config head

## Paths

Relative to **`agentic/`**.

| Path | Allowed |
|---|---|
| `config.yaml` | **read** via `./ralph/bin/read-lines.sh` |

Read first 10 lines of config.yaml

- [x] ./ralph/bin/read-lines.sh config.yaml 1 10
- [x] ./ralph/bin/gate-0001.sh

## Status

COMPLETE

