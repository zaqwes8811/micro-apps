# Class to add

Сделай класс кэша, для строк, нужно только класть строки

Один метод вставка возвращающая бул, если ключ был вставлен первый раз

Каждый вызов инкрементирует счетчики вставленных до этого

Если ключ больше заданного числа, ключ из кеша удаляется, число передать в конструкторе

Код на питоне

# Использование

Добавь инстанс кэша для HanziDB для метода get_random_100, получать новое значение 
до тех пор пока кеш не вернет true при вставке поля charcter

Количество запросов ограничь 20-ю, не бесконечный цикл

# Другие кеши

сделай то же для get_random, кэш отдельный, количество запросов сделать общим
в качестве константы

# Отдельный кеш по радикалам

Сделать отдельный кеш для get_random_radical, но поле для обращения к кэшу будет radical

# Финальное расположение

перенеси клаш кеша в файл hanzi_db.py